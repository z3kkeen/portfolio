"use client";
import React from "react";
import StatsTitle from "./stats-title";

export default function Stats() {
  return (
    <section id="stats" className="h-screen w-full flex flex-col items-center ">
      <StatsTitle />
    </section>
  );
}
